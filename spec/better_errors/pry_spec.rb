require "spec_helper"

describe BetterErrors::Pry do
  it "has a version number" do
    expect(BetterErrors::Pry::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
