source 'https://rubygems.org'

# Specify your gem's dependencies in better_errors-pry.gemspec
gemspec

# gem "pry-byebug"

# Use the current master of the project, since the latest release does not support this gem.
# TODO: move this to Appraisals and add an alternate version for the latest release.
gem 'better_errors', github: 'charliesome/better_errors'

# Allows us to generate multiple Gemfiles to run tests against multiple versions of dependencies.
gem "appraisal"
